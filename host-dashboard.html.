<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rotel – Host Dashboard</title>

    <style>
        body {
            margin: 0;
            font-family: Arial, Helvetica, sans-serif;
            background: #ffffff;
        }

        /* Navbar */
        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 18px 40px;
            background-color: #ffffff;
            border-bottom: 1px solid #e0e0e0;
        }

        .logo {
            font-size: 22px;
            font-weight: bold;
            color: #6b6bff;
        }

        .nav-links {
            list-style: none;
            display: flex;
            gap: 25px;
            margin: 0;
        }

        .nav-links a {
            text-decoration: none;
            color: #555;
            font-size: 16px;
        }

        .nav-links a:hover {
            color: #6b6bff;
        }

        /* Page Container */
        .page-container {
            padding: 40px 50px;
        }

        h2 {
            font-size: 32px;
            margin-bottom: 25px;
        }

        /* Tabs */
        .tabs {
            display: flex;
            gap: 30px;
            margin-bottom: 30px;
            border-bottom: 1px solid #e0e0e0;
            padding-bottom: 10px;
        }

        .tab {
            font-size: 16px;
            cursor: pointer;
            color: #6b6bff;
            font-weight: bold;
        }

        .tab.inactive {
            color: #777;
        }

        /* Form Styling */
        form {
            max-width: 700px;
        }

        label {
            display: block;
            font-size: 14px;
            color: #555;
            margin-bottom: 6px;
            margin-top: 18px;
        }

        input, textarea, select {
            width: 100%;
            padding: 12px 15px;
            border: 1px solid #ccc;
            border-radius: 10px;
            font-size: 16px;
        }

        textarea {
            resize: vertical;
            height: 100px;
        }

        .submit-btn {
            margin-top: 25px;
            padding: 12px 22px;
            background: #6b6bff;
            border: none;
            border-radius: 25px;
            color: white;
            font-size: 16px;
            cursor: pointer;
        }

        .submit-btn:hover {
            background: #5757e6;
        }

    </style>
</head>

<body>

    <!-- NAVBAR -->
    <nav class="navbar">
        <div class="logo">Rotel</div>
        <ul class="nav-links">
            <li><a href="#">Home</a></li>
            <li><a href="#">Search</a></li>
            <li><a href="#">Host Dashboard</a></li>
            <li><a href="#">About</a></li>
            <li><a href="#">Contact</a></li>
            <li><a href="#">Login</a></li>
        </ul>
    </nav>

    <!-- PAGE CONTENT -->
    <div class="page-container">

        <h2>Host Dashboard</h2>

        <!-- Tabs -->
        <div class="tabs">
            <div class="tab">Create Listing</div>
            <div class="tab inactive">Manage Bookings</div>
        </div>

        <!-- Listing Form -->
        <form>

            <label>Property Title</label>
            <input type="text" placeholder="e.g., Cozy Downtown Apartment">

            <label>Location</label>
            <input type="text" placeholder="City, Country">

            <label>Description</label>
            <textarea placeholder="Describe your property..."></textarea>

            <label>Price per Night (£)</label>
            <input type="number" placeholder="68">

            <label>Property Type</label>
            <select>
                <option>Select type</option>
                <option>Apartment</option>
                <option>Villa</option>
                <option>Studio</option>
                <option>House</option>
                <option>Guest Suite</option>
            </select>

            <button class="submit-btn">Create Listing</button>

        </form>

    </div>

</body>
</html>
